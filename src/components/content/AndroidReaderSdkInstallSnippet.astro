---
import { CodeBlock } from "@components/Code";
import { getAndroidReaderSdkVersion } from "@lib/sdks/androidSdkVersions";

const androidReaderSdkVersion = await getAndroidReaderSdkVersion();
---

<CodeBlock
  language="groovy"
  code={`implementation 'com.sumup:merchant-sdk:${androidReaderSdkVersion}'`}
/>
