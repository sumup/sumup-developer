---
import MarkdownContent from "@components/MarkdownContent.astro";
---

{
  Astro.locals.starlightRoute.custom ? (
    <slot />
  ) : (
    <MarkdownContent>
      <slot />
    </MarkdownContent>
  )
}
