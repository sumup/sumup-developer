---
title: Missing Token
id: missing-token
description: Understand the SumUp API Missing Token response, common causes, and example payload for unauthenticated requests.
---

Indicates that the request has not been applied because it lacks valid authentication credentials for the target resource.

## Common Causes

- The `Authorization` header is missing.
- The token is not included in the request at all.
- The header is present but empty or malformed.

```json
{
  "message": "access token required",
  "error_code": "NOT_AUTHORIZED"
}
```
